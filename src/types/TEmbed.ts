export type TEmbed = { type: "CHANGE_OUTPUT_QUALITY"; payload: { outputQuality: string } } |
{ type: "UPDATE_FREE_SPACE"; payload: { totalSecretFileSize: number, initFreeSpace: number } } |
{ type: "CLEAR"; };

